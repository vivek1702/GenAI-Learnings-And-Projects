# Ignore all .env files anywhere in the repo
.env
**/.env

# Optional: ignore node_modules everywhere
node_modules
**/node_modules


# .env ignores a .env in the root.
# **/.env ignores .env files in any subfolder (firstClass, secondClass, thirdClass, etc.).